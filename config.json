{
    "name": "OptGroup",
        "namespace": "INTERSECT\\OptGroup",
		"description": "Allows designers to build dropdown boxes with option groups; sections of choices, by annotating a dropdown field with <code>@OPTGROUP='x,y,z'</code>, where <code>x</code>, <code>y</code> and <code>z</code> represent choices whose labels become the group labels.",
		"framework-version": 14,
		"authors": [
			{
				"name": "Aidan Wilson",
				"email": "aidan.wilson@intersect.org.au",
				"institution": "Intersect Australia"
			}
		],
		"action-tags": [
			{
				"tag": "@OPTGROUP", 
				"description": "Displays selected dropdown choices as option group labels, wherein the synyax <code>@OPTGROUP='x,y,z'</code> means that choices <code>x</code>, <code>y</code> and <code>z</code> are not selectable, but instead their labels are used as the group labels of the other options."
			}
        ]
}
